# SugarCRM Testing Skill — Environment Variables
# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# BNS tools directory (contains bin/, tests/, fields.txt, etc.)
TOOLS_PATH=~/dev/bns/tools

# Sugar projects root directory (each project is a subdirectory)
PROJECTS_PATH=~/sites
